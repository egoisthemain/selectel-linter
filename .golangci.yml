version: "2"

linters:
  default: none
  enable:
    - loglint

  settings:
    custom:
      loglint:
        type: "module"
        description: "Checks slog/zap log messages for style and safety rules"
        settings: {}